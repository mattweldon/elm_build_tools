{
  "name": "chokidar",
  "version": "1.0.0",
  "description": "Compile multiple Elm modules using Chokidar to watch for changed files.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "chokidar 'src/*.elm' -c 'elm make src/Main.elm --output public/main.js' --initial"
  },
  "author": "Matt Weldon",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^1.2.0"
  }
}
